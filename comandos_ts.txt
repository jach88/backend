una vez instalado el tsc (typescript)
yarn tsc => buscara el tsconfig.json para que de acuerdo a ello compilar todos los archivos de formato typescript
yarn tsc nombre_archivo.ts => compilara el archivo tsc sin importar si tenemos el archivo tsconfig.json y usará las configuraciones por defecto de tsc
yarn tsc --init => creara el archivo tsconfig.json en el directorio actual con las opciones recomendadas del compilador de tsc con sus comentarios de para que sirve cada opcion